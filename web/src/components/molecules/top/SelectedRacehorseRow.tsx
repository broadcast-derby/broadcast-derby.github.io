import { styled } from '@mui/material/styles'

// mui
import Grid from '@mui/material/Grid'

/**
 * 選択した出走馬の一覧レイアウト
 */
const SelectedRacehorseRow = styled(Grid)({
  marginBottom: "5px",
})

export default SelectedRacehorseRow