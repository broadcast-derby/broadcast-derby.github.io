import { styled } from '@mui/material/styles'

// mui
import ListItemText from '@mui/material/ListItemText'

/**
 * 出走馬名テキスト
 */
const RacehorseNameText = styled(ListItemText)({
  marginTop: "14px",
})

export default RacehorseNameText